路径【D:\home\depot\depot-apply\src\main\resources\mapper\base】
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lsz.apply.base.mapper【点包名】.【Uname】Mapper">
    <sql id="Base_Column_List">
        【select_column】
    </sql>

    <!-- 表的扩展字段 -->
    <sql id="Extend_Column_List">
    </sql>

    <!-- 外连接表 -->
    <sql id="Left_Join_Table">
    </sql>

    <select id="page【Uname】" resultType="com.lsz.model.vo【点包名】.【Uname】VO">
        select
        <include refid="Base_Column_List"/>
        <include refid="Extend_Column_List"/>
        FROM 【tableName】 ta
        <include refid="Left_Join_Table"/>
        <where>
            【selectWhere】

            <if test="dto.idList != null">
                <foreach collection="dto.idList" item="item" open="AND ta.id IN(" separator="," close=")">
                    #{item}
                </foreach>
            </if>

            <if test="null != dto.startTime">
                <![CDATA[
            AND ta.create_time >= #{dto.startTime}
            AND ta.create_time < #{dto.endTime}
            ]]>
            </if>
        </where>
    </select>

    <select id="detail【Uname】" resultType="com.lsz.model.vo【点包名】.【Uname】VO">
        select
        <include refid="Base_Column_List"/>
        <include refid="Extend_Column_List"/>
        FROM 【tableName】 ta
        <include refid="Left_Join_Table"/>
        <where>
            <if test="dto.id != null and dto.id != ''">
                AND ta.id = #{dto.id}
            </if>
        </where>
    </select>
</mapper>
